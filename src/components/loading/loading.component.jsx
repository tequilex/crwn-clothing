import { Spinner, LoadingContainer } from "./loading.styles";

const Loading = () => {
  return (
    <LoadingContainer>
      <Spinner></Spinner>
    </LoadingContainer>
  );
};
export default Loading;
